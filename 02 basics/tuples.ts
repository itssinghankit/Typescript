let user: [number, string, boolean];
user = [1, "Ankit", true];

let rgb: [number, number, number];
rgb = [255, 0, 0.0];

//array of array
let arr: [number, string, boolean][] = [
  [1, "Ankit", true],
  [2, "Pushkar", false],
];

user[1]= "pushkar"

//tuples caution
//it is also possible to push new value in tuple
user.push("new value")